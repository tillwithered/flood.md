# flood.md project workspace snapshot

This `.flood` directory is a versioned repository snapshot of project-owned Rules and Skills exported from flood.md.

The live flood MCP project workspace remains canonical until explicit bidirectional sync semantics are implemented.

Do not store secrets, tokens, or sessions in this snapshot.